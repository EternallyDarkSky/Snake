{
    "compilerOptions": {
        "target":"es2015",
        "module": "es2015", 
        "strict": true,
         // 编译的时候删除注释
        "removeComments": false,
        // 有错误时，不生成编译文件
        "noEmitOnError": true,
        // 检测this是否隐式指定,不允许不明确类型的this
        "noImplicitThis": false,
    }
}